services:
  - type: web
    name: task-manager-backend
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn backend.main:app --bind 0.0.0.0:${PORT:-5000} --workers 4
