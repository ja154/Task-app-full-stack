# Example .env file for collaborators (DO NOT commit secrets here)
# Copy this to `.env` and fill in secure values.

# SQLAlchemy DATABASE_URL (SQLAlchemy-format connection string)
# For local development using SQLite (recommended):
DATABASE_URL=sqlite:///backend/instance/taskmanager.db

# JWT secret used to sign access tokens. Use a long, random string.
JWT_SECRET_KEY=your-secure-jwt-secret-here

# Optional: other env variables
# FLASK_ENV=development